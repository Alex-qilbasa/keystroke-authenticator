# keystroke-authenticator
This a project, it can help ion authentication in addition to initial. It works by reading and remembering the keystroke pattern of the user and while the device is loggeds in so it analyzes the keystrokes while the user is working and analyze itt, if it is different then we can the initial login was bypassed
